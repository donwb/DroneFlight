# Media + generated data — NEVER commit these (they are ~10GB and grow).
# They reach the server via rsync (publish.sh), not git.
flights/
manifest.json

# Python / OS cruft
__pycache__/
*.pyc
.DS_Store

# local editor/runtime
.claude/
*.log
